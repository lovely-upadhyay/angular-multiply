{
    "name": "myapp",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "start": "nodemon --ignore README.md --watch server.js --watch database.json server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "cors": "^2.8.4",
        "express": "^4.16.3",
        "mysql": "^2.15.0"
    },
    "devDependencies": {
        "nodemon": "^1.17.5"
    }
}
